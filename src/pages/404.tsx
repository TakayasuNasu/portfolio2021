import React from 'react'
import type { FC } from 'react'

// components
import Layout from '@/components/layout'

const Page: FC = () => (
  <Layout>
    <span>404</span>
  </Layout>
)
export default Page
